version: '3.7'
services:
  app_test:
    build: .
    image: balexander85/wrapped_driver_test
    container_name: driver_test
    command: /wrapped-driver-env/bin/python -m pytest -vv .
    volumes:
     - ../wrapped_driver/wrapped_driver.py:/usr/src/wrapped_driver.py

